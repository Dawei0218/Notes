# 重载运算符

### 重载

```cpp
// :: .* . ?: & ｜ , 不能重载
// && ｜｜无法保留内置运算符的短路求值属性
// = [] () -> 必须是成员函数
// >> << 必须是非成员函数
class S
{
  S &operator=(const S &s1) {}
  S &operator--() {} // 前置--
  S operator--(int) {} // 后置--
}
```

